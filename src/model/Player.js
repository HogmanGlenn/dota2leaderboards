export class Player {
    constructor(countryCode, name, rank, teamId, teamTag) {
        this.countryCode = countryCode;
        this.name = name;
        this.rank = rank;
        this.team_id = teamId;
        this.team_tag = teamTag;
    }
}